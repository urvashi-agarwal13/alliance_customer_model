name: 'alliance_customer_model'
version: '1.0'
config-version: 2
profile: alliance_customer_model
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  alliance_customer_model:
    +database: DEV
    +schema: 
    staging:
      +materialized: view
    datavault:
      +materialized: table

seeds:
  alliance_customer_model:
    customers:
      +alias: raw_customer
    products:
      +alias: raw_product
    customer_products:
      +alias: raw_customer_product
