name: 'alliance_customer_model'
version: '1.0'
config-version: 2
profile: alliance_customer_model
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  alliance_customer_model:
    +database: DEV
    +schema: SALES
    staging:
      +materialized: view
    datavault:
      +materialized: table
